!(function () {
  const e =
      "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVheGFzaHhwcXBpaG9ubnVoZHB4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzI0NDk4MzMsImV4cCI6MjA4ODAyNTgzM30.j8n-puyJ6rKDMibSCxteJeWbVpI7xkxFux_njkHXlGg",
    t = "https://eaxashxpqpihonnuhdpx.supabase.co/functions/v1",
    n = t + "/get-applications",
    a = t + "/admin-update-application",
    o = t + "/admin-delete-application",
    i = t + "/admin-create-application",
    r = t + "/admin-update-experience",
    l = t + "/admin-delete-experience",
    d = t + "/admin-create-experience",
    c = t + "/admin-manage-schools",
    s = t + "/admin-upload-applications",
    p = t + "/admin-upload-experiences",
    Y = t + "/check-admin-role",
    u = 25;
  let m = [],
    h = [],
    f = 1,
    y = 1;
  const v =
      '<svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="none"\n    stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/>\n    <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/>\n  </svg>',
    x =
      '<svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="none"\n    stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <polyline points="3 6 5 6 21 6"/>\n    <path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"/>\n    <path d="M10 11v6M14 11v6"/>\n    <path d="M9 6V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/>\n  </svg>';
  function C(e, t, n) {
    const a = document.createElement("button");
    return (
      (a.type = "button"),
      (a.className = "adm-btn " + t),
      a.setAttribute("aria-label", n),
      (a.innerHTML = e),
      a
    );
  }
  function g(e) {
    const t = document.createElement("div");
    t.style.cssText = "display:flex;align-items:center;gap:8px;";
    const n = C(v, "edit", "Edit"),
      o = C(x, "delete", "Delete");
    return (
      n.addEventListener("click", (t) => {
        (t.preventDefault(),
          t.stopPropagation(),
          (function (e) {
            const t = S("adm-app-modal"),
              n = t.querySelector(".adm-modal");
            ((n.innerHTML =
              '\n      <div class="adm-modal-header">\n        <span class="adm-modal-title">Edit Application</span>\n        <button class="adm-modal-close" aria-label="Close">&times;</button>\n      </div>'),
              n
                .querySelector(".adm-modal-close")
                .addEventListener("click", () => N(t)));
            const o = document.createElement("div");
            o.className = "adm-modal-body";
            const i = document.createElement("div");
            i.className = "adm-form-grid";
            const r = {},
              l = L("text", e.pseudonym),
              d = L("text", e.alias),
              c = L("text", e.undergrad_institution),
              s = L("text", e.state),
              p = L("number", e.gpa, "0.01", "0", "4.0"),
              u = L("number", e.mcat, null, "472", "528"),
              h = L("number", e.gap_years, null, "0"),
              f = L("number", e.interviews_count, null, "0"),
              y = L("number", e.acceptances_count, null, "0"),
              v = L("number", e.scholarships_count, null, "0"),
              x = L(
                "number",
                e.application_cycle_start_year,
                null,
                "2000",
                "2100",
              ),
              C = L(
                "number",
                e.application_cycle_end_year,
                null,
                "2000",
                "2100",
              ),
              g = L("text", e.ses_disadvantaged),
              b = T(
                [
                  ["published", "Published"],
                  ["draft", "Draft"],
                  ["archived", "Archived"],
                ],
                e.status || "published",
              );
            ((r.pseudonym = l),
              (r.alias = d),
              (r.undergrad_institution = c),
              (r.state = s),
              (r.gpa = p),
              (r.mcat = u),
              (r.gap_years = h),
              (r.interviews_count = f),
              (r.acceptances_count = y),
              (r.scholarships_count = v),
              (r.application_cycle_start_year = x),
              (r.application_cycle_end_year = C),
              (r.ses_disadvantaged = g),
              (r.status = b),
              i.appendChild(I("Identity")),
              i.appendChild(A("Pseudonym *", l)),
              i.appendChild(A("Alias *", d)),
              i.appendChild(I("Academic")),
              i.appendChild(A("Undergrad Institution *", c, !0)),
              i.appendChild(A("State *", s)),
              i.appendChild(A("GPA *", p)),
              i.appendChild(A("MCAT *", u)),
              i.appendChild(I("Application Cycle")),
              i.appendChild(A("Cycle Start Year *", x)),
              i.appendChild(A("Cycle End Year *", C)),
              i.appendChild(A("Gap Years", h)),
              i.appendChild(I("Outcomes")),
              i.appendChild(A("Interviews", f)),
              i.appendChild(A("Acceptances", y)),
              i.appendChild(A("Scholarships", v)),
              i.appendChild(I("Profile")),
              i.appendChild(A("SES Disadvantaged *", g, !0)),
              i.appendChild(A("Status", b)));
            const E = [
                [
                  "on_time_application",
                  "On-Time Application",
                  e.on_time_application,
                ],
                ["reapplicant", "Reapplicant", e.reapplicant],
                ["first_generation", "First Generation", e.first_generation],
                ["military_service", "Military Service", e.military_service],
                [
                  "institutional_actions",
                  "Institutional Actions",
                  e.institutional_actions,
                ],
              ],
              _ = {};
            (i.appendChild(I("Flags")),
              E.forEach(([e, t, n]) => {
                const { wrap: a, chk: o } = M(t, n);
                ((_[e] = o), i.appendChild(a));
              }));
            const { el: q, getSchools: D } = F(e.application_schools || []);
            (i.appendChild(q), o.appendChild(i), n.appendChild(o));
            const H = document.createElement("div");
            H.className = "adm-modal-footer";
            const O = document.createElement("button");
            ((O.className = "adm-btn-cancel"),
              (O.textContent = "Cancel"),
              O.addEventListener("click", () => N(t)));
            const B = document.createElement("button");
            ((B.className = "adm-btn-save"),
              (B.textContent = "Save Changes"),
              B.addEventListener("click", async () => {
                let n = !1;
                ([l, d, c, s, g].forEach((e) => {
                  e.value.trim()
                    ? (e.style.borderColor = "")
                    : ((e.style.borderColor = "#ef4444"),
                      n || (e.focus(), (n = !0)));
                }),
                  [x, C].forEach((e) => {
                    "" === e.value
                      ? ((e.style.borderColor = "#ef4444"),
                        n || (e.focus(), (n = !0)))
                      : (e.style.borderColor = "");
                  }));
                const o = parseFloat(p.value);
                "" === p.value || isNaN(o) || o < 0 || o > 4
                  ? ((p.style.borderColor = "#ef4444"),
                    (p.title = "GPA must be between 0.00 and 4.00"),
                    n || (p.focus(), (n = !0)))
                  : ((p.style.borderColor = ""), (p.title = ""));
                const i = parseInt(u.value, 10);
                if (
                  ("" === u.value || isNaN(i) || i < 472 || i > 528
                    ? ((u.style.borderColor = "#ef4444"),
                      (u.title = "MCAT must be between 472 and 528"),
                      n || (u.focus(), (n = !0)))
                    : ((u.style.borderColor = ""), (u.title = "")),
                  n)
                )
                  return;
                ((B.disabled = !0), (B.textContent = "Saving…"));
                const h = { id: e.id };
                (Object.entries(r).forEach(([e, t]) => {
                  h[e] =
                    "SELECT" === t.tagName
                      ? t.value
                      : "" === t.value
                        ? null
                        : t.value;
                }),
                  Object.entries(_).forEach(([e, t]) => {
                    h[e] = t.checked;
                  }),
                  ["gpa"].forEach((e) => {
                    null !== h[e] && (h[e] = parseFloat(h[e]));
                  }),
                  [
                    "mcat",
                    "gap_years",
                    "interviews_count",
                    "acceptances_count",
                    "scholarships_count",
                    "application_cycle_start_year",
                    "application_cycle_end_year",
                  ].forEach((e) => {
                    null !== h[e] && (h[e] = parseInt(h[e], 10));
                  }));
                const [f, y] = await Promise.all([P(a, h), j(e.id, D())]);
                if (f) {
                  const n = m.findIndex((t) => t.id === e.id);
                  (-1 !== n &&
                    ((m[n] = { ...m[n], ...h }),
                    y && (m[n].application_schools = y.schools || [])),
                    w(),
                    N(t));
                } else
                  ((B.disabled = !1),
                    (B.textContent = "Save Changes"),
                    alert("Failed to save. Please try again."));
              }),
              H.appendChild(O),
              H.appendChild(B),
              n.appendChild(H),
              k(t));
          })(e));
      }),
      o.addEventListener("click", (t) => {
        (t.preventDefault(), t.stopPropagation(), q("application", e.id));
      }),
      t.appendChild(n),
      t.appendChild(o),
      t
    );
  }
  function b(e) {
    const t = document.createElement("div");
    t.style.cssText = "display:flex;align-items:center;gap:8px;";
    const n = C(v, "edit", "Edit"),
      a = C(x, "delete", "Delete");
    return (
      n.addEventListener("click", (t) => {
        (t.preventDefault(),
          t.stopPropagation(),
          (function (e) {
            const t = S("adm-exp-modal"),
              n = t.querySelector(".adm-modal");
            ((n.innerHTML =
              '\n      <div class="adm-modal-header">\n        <span class="adm-modal-title">Edit Experience</span>\n        <button class="adm-modal-close" aria-label="Close">&times;</button>\n      </div>'),
              n
                .querySelector(".adm-modal-close")
                .addEventListener("click", () => N(t)));
            const a = document.createElement("div");
            a.className = "adm-modal-body";
            const o = document.createElement("div");
            o.className = "adm-form-grid";
            const i = L("text", e.title),
              l = L("text", e.experience_type),
              d = T(
                [
                  ["", "— Select type —"],
                  ["additional", "Additional"],
                  ["most_meaningful", "Most Meaningful"],
                ],
                e.category || "",
              ),
              c = L("number", e.hours, null, "0"),
              s = L("number", e.sort_order, null, "1"),
              p = document.createElement("textarea");
            ((p.className = "adm-textarea"),
              (p.value = e.description || ""),
              (p.rows = 5),
              o.appendChild(A("Title", i, !0)),
              o.appendChild(A("Category", l)),
              o.appendChild(A("Type", d)),
              o.appendChild(A("Hours", c)),
              o.appendChild(A("Sort Order", s)),
              o.appendChild(A("Description", p, !0)),
              a.appendChild(o),
              n.appendChild(a));
            const u = document.createElement("div");
            u.className = "adm-modal-footer";
            const m = document.createElement("button");
            ((m.className = "adm-btn-cancel"),
              (m.textContent = "Cancel"),
              m.addEventListener("click", () => N(t)));
            const f = document.createElement("button");
            ((f.className = "adm-btn-save"),
              (f.textContent = "Save Changes"),
              f.addEventListener("click", async () => {
                ((f.disabled = !0), (f.textContent = "Saving…"));
                const n = {
                  id: e.id,
                  title: i.value || null,
                  experience_type: l.value || null,
                  category: d.value || null,
                  hours: "" !== c.value ? parseInt(c.value, 10) : null,
                  sort_order: "" !== s.value ? parseInt(s.value, 10) : null,
                  description: p.value || null,
                };
                if (await P(r, n)) {
                  const a = h.findIndex((t) => t.id === e.id);
                  (-1 !== a && (h[a] = { ...h[a], ...n }), E(), N(t));
                } else
                  ((f.disabled = !1),
                    (f.textContent = "Save Changes"),
                    alert("Failed to save. Please try again."));
              }),
              u.appendChild(m),
              u.appendChild(f),
              n.appendChild(u),
              k(t));
          })(e));
      }),
      a.addEventListener("click", (t) => {
        (t.preventDefault(), t.stopPropagation(), q("experience", e.id));
      }),
      t.appendChild(n),
      t.appendChild(a),
      t
    );
  }
  function w() {
    const e = document.querySelector('[data-app-list="rows"]');
    if (!e) return;
    const t = e.querySelector('[data-app-row="template"]');
    if (!t) return;
    e.querySelectorAll('[data-app-row="item"]').forEach((e) => e.remove());
    const n =
      ((a = m),
      (o = U("normal"))
        ? a.filter(function (e) {
            return (function (e, t) {
              if (!t) return !0;
              const n = [
                e.pseudonym,
                e.alias,
                e.undergrad_institution,
                e.state,
                e.gpa,
                e.mcat,
                e.gap_years,
                e.interviews_count,
              ].map(function (e) {
                return String(null != e ? e : "").toLowerCase();
              });
              return n.some(function (e) {
                return e.includes(t);
              });
            })(e, o);
          })
        : a);
    var a, o;
    const i = (f - 1) * u;
    (n.slice(i, i + u).forEach((n) => {
      const a = t.cloneNode(!0);
      (a.setAttribute("data-app-row", "item"),
        a.removeAttribute("hidden"),
        (a.style.display = "grid"));
      const o = a.querySelector('[data-cell="student-name"]'),
        i = a.querySelector('[data-cell="alias"]'),
        r = a.querySelector('[data-cell="undergrad"]'),
        l = a.querySelector('[data-cell="actions"]');
      (o && (o.textContent = n.pseudonym || "-"),
        i && (i.textContent = n.alias || "-"),
        r && (r.textContent = n.undergrad_institution || "-"),
        l && ((l.innerHTML = ""), l.appendChild(g(n))),
        e.appendChild(a));
    }),
      _("app", n.length, f, (e) => {
        ((f = e), w());
      }));
  }
  function E() {
    const e = document.querySelector('[data-exp-list="rows"]');
    if (!e) return;
    const t = e.querySelector('[data-exp-row="template"]');
    if (!t) return;
    e.querySelectorAll('[data-exp-row="item"]').forEach((e) => e.remove());
    const n =
      ((a = h),
      (o = U("experiences"))
        ? a.filter(function (e) {
            return (function (e, t) {
              if (!t) return !0;
              const n = [
                e.student_name,
                e.pseudonym,
                e.title,
                e.experience_type,
                e.category,
                e.hours,
              ].map(function (e) {
                return String(null != e ? e : "").toLowerCase();
              });
              return n.some(function (e) {
                return e.includes(t);
              });
            })(e, o);
          })
        : a);
    var a, o;
    const i = (y - 1) * u;
    (n.slice(i, i + u).forEach((n) => {
      const a = t.cloneNode(!0);
      (a.setAttribute("data-exp-row", "item"),
        a.removeAttribute("hidden"),
        (a.style.display = "grid"));
      const o = a.querySelector('[data-cell="student-name"]'),
        i = a.querySelector('[data-cell="experience-type"]'),
        r = a.querySelector('[data-cell="title"]'),
        l = a.querySelector('[data-cell="actions"]');
      (o && (o.textContent = n.student_name || "-"),
        i && (i.textContent = n.experience_type || "-"),
        r && (r.textContent = n.title || "-"),
        l && ((l.innerHTML = ""), l.appendChild(b(n))),
        e.appendChild(a));
    }),
      _("exp", n.length, y, (e) => {
        ((y = e), E());
      }));
  }
  function _(e, t, n, a) {
    const o = "adm-pagination-" + e;
    let i = document.getElementById(o);
    if (!i) {
      ((i = document.createElement("div")),
        (i.id = o),
        (i.className = "adm-pagination"));
      const t = document.querySelector(
        "app" === e ? '[data-app-list="rows"]' : '[data-exp-list="rows"]',
      );
      t && t.parentElement && t.parentElement.appendChild(i);
    }
    const r = Math.max(1, Math.ceil(t / u)),
      l = Math.min((n - 1) * u + 1, t),
      d = Math.min(n * u, t);
    ((i.innerHTML = `\n      <span class="adm-pagination-info">\n        Showing <strong>${l}–${d}</strong> of <strong>${t}</strong>\n      </span>\n      <div class="adm-pagination-controls">\n        <button class="adm-page-btn" id="${o}-prev" ${n <= 1 ? "disabled" : ""}>&#8592; Prev</button>\n        <span style="font-size:13px;color:#374151;font-weight:600;">${n} / ${r}</span>\n        <button class="adm-page-btn" id="${o}-next" ${n >= r ? "disabled" : ""}>Next &#8594;</button>\n      </div>`),
      document
        .getElementById(o + "-prev")
        .addEventListener("click", () => a(n - 1)),
      document
        .getElementById(o + "-next")
        .addEventListener("click", () => a(n + 1)));
  }
  function S(e) {
    let t = document.getElementById(e);
    if (t) return t;
    ((t = document.createElement("div")),
      (t.id = e),
      (t.className = "adm-modal-overlay"),
      t.addEventListener("click", (e) => {
        e.target === t && N(t);
      }));
    const n = document.createElement("div");
    return (
      (n.className = "adm-modal"),
      t.appendChild(n),
      document.body.appendChild(t),
      t
    );
  }
  function k(e) {
    (e.classList.add("visible"), (document.body.style.overflow = "hidden"));
  }
  function N(e) {
    (e.classList.remove("visible"), (document.body.style.overflow = ""));
  }
  function A(e, t, n) {
    const a = document.createElement("div");
    a.className = "adm-field" + (n ? " full" : "");
    const o = document.createElement("label");
    if (((o.className = "adm-label"), e.endsWith(" *"))) {
      o.textContent = e.slice(0, -2) + " ";
      const t = document.createElement("span");
      ((t.className = "req"), (t.textContent = "*"), o.appendChild(t));
    } else o.textContent = e;
    return (a.appendChild(o), a.appendChild(t), a);
  }
  function L(e, t, n, a, o) {
    const i = document.createElement("input");
    return (
      (i.type = e || "text"),
      (i.className = "adm-input"),
      (i.value = null != t ? t : ""),
      null != n && (i.step = n),
      null != a && (i.min = a),
      null != o && (i.max = o),
      i
    );
  }
  function T(e, t) {
    const n = document.createElement("select");
    return (
      (n.className = "adm-select"),
      e.forEach(([e, a]) => {
        const o = document.createElement("option");
        ((o.value = e),
          (o.textContent = a),
          e === t && (o.selected = !0),
          n.appendChild(o));
      }),
      n
    );
  }
  function M(e, t) {
    const n = document.createElement("div");
    n.className = "adm-toggle-row full";
    const a = document.createElement("span");
    ((a.className = "adm-toggle-label"), (a.textContent = e));
    const o = document.createElement("input");
    return (
      (o.type = "checkbox"),
      (o.className = "adm-toggle"),
      (o.checked = !!t),
      n.appendChild(a),
      n.appendChild(o),
      { wrap: n, chk: o }
    );
  }
  function I(e) {
    const t = document.createElement("div");
    return ((t.className = "adm-section-title"), (t.textContent = e), t);
  }
  function q(t, n) {
    let a = document.getElementById("adm-delete-confirm");
    (a ||
      ((a = document.createElement("div")),
      (a.id = "adm-delete-confirm"),
      (a.className = "adm-confirm-overlay"),
      (a.innerHTML = `\n        <div class="adm-confirm-box">\n          <div class="adm-confirm-icon"><svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none"\n    stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <polyline points="3 6 5 6 21 6"/>\n    <path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"/>\n    <path d="M10 11v6M14 11v6"/>\n    <path d="M9 6V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/>\n  </svg></div>\n          <div class="adm-confirm-title">Delete Record</div>\n          <div class="adm-confirm-text">This action cannot be undone. Are you sure you want to delete this ${t}?</div>\n          <div class="adm-confirm-actions">\n            <button class="adm-btn-cancel" id="adm-delete-cancel">Cancel</button>\n            <button class="adm-btn-danger" id="adm-delete-confirm-btn">Delete</button>\n          </div>\n        </div>`),
      a.addEventListener("click", (e) => {
        e.target === a && c();
      }),
      document.body.appendChild(a)),
      (a.querySelector(".adm-confirm-text").textContent =
        `This action cannot be undone. Are you sure you want to delete this ${t}?`));
    const i = a.querySelector("#adm-delete-cancel"),
      r = a.querySelector("#adm-delete-confirm-btn"),
      d = r.cloneNode(!0);
    function c() {
      (a.classList.remove("visible"), (document.body.style.overflow = ""));
    }
    (r.parentNode.replaceChild(d, r),
      (d.disabled = !1),
      (d.textContent = "Delete"),
      (i.onclick = c),
      d.addEventListener("click", async () => {
        ((d.disabled = !0), (d.textContent = "Deleting…"));
        const a = "application" === t ? o : l,
          i = await (async function (t, n) {
            try {
              const a = await fetch(t, {
                method: "POST",
                headers: {
                  "Content-Type": "application/json",
                  apikey: e,
                  Authorization: "Bearer " + e,
                },
                body: JSON.stringify({ id: n }),
              });
              if (!a.ok) {
                const e = await a.json();
                return (console.error("Delete error:", e), !1);
              }
              return !0;
            } catch (e) {
              return (console.error("Delete error:", e), !1);
            }
          })(a, n);
        i
          ? ("application" === t
              ? ((m = m.filter((e) => e.id !== n)),
                (h = h.filter((e) => e.application_id !== n)),
                f > Math.ceil(m.length / u) && (f = Math.max(1, f - 1)),
                y > Math.ceil(h.length / u) && (y = Math.max(1, y - 1)),
                w(),
                E())
              : ((h = h.filter((e) => e.id !== n)),
                y > Math.ceil(h.length / u) && (y = Math.max(1, y - 1)),
                E()),
            c())
          : ((d.disabled = !1),
            (d.textContent = "Delete"),
            alert("Failed to delete. Please try again."));
      }),
      a.classList.add("visible"),
      (document.body.style.overflow = "hidden"));
  }
  async function P(t, n) {
    try {
      const a = await fetch(t, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          apikey: e,
          Authorization: "Bearer " + e,
        },
        body: JSON.stringify(n),
      });
      if (!a.ok) {
        const e = await a.json();
        return (console.error("Update error:", e), !1);
      }
      return !0;
    } catch (e) {
      return (console.error("Update error:", e), !1);
    }
  }
  async function D(t, n) {
    try {
      const a = await fetch(t, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          apikey: e,
          Authorization: "Bearer " + e,
        },
        body: JSON.stringify(n),
      });
      if (!a.ok) {
        const e = await a.json();
        return (console.error("Create error:", e), null);
      }
      return await a.json();
    } catch (e) {
      return (console.error("Create error:", e), null);
    }
  }
  async function j(t, n) {
    try {
      const a = await fetch(c, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          apikey: e,
          Authorization: "Bearer " + e,
        },
        body: JSON.stringify({ application_id: t, schools: n }),
      });
      if (!a.ok) {
        const e = await a.json();
        return (console.error("Schools error:", e), null);
      }
      return await a.json();
    } catch (e) {
      return (console.error("Schools error:", e), null);
    }
  }
  function F(e) {
    e = (e || [])
      .slice()
      .sort((e, t) => (e.sort_order || 0) - (t.sort_order || 0));
    const t = document.createElement("div");
    t.style.gridColumn = "1 / -1";
    const n = document.createElement("div");
    n.style.cssText =
      "display:flex;align-items:center;justify-content:space-between;margin:14px 0 8px;";
    const a = document.createElement("span");
    ((a.style.cssText =
      "font-size:12px;font-weight:700;color:#374151;text-transform:uppercase;letter-spacing:.06em;"),
      (a.textContent = "Schools"));
    const o = document.createElement("button");
    ((o.type = "button"),
      (o.textContent = "+ Add School"),
      (o.style.cssText =
        "background:#FFA25F;color:#fff;border:none;border-radius:6px;padding:4px 12px;font-size:12px;cursor:pointer;font-weight:600;"),
      o.addEventListener("click", () => s()),
      n.appendChild(a),
      n.appendChild(o),
      t.appendChild(n));
    const i = document.createElement("div");
    i.style.cssText =
      "display:flex;gap:8px;align-items:flex-start;margin-bottom:10px;";
    const r = document.createElement("textarea");
    ((r.placeholder =
      "Paste comma-separated school names here, then click Import…"),
      (r.rows = 2),
      (r.style.cssText =
        "flex:1;font-size:12px;padding:6px 8px;border:1px solid #d1d5db;border-radius:6px;resize:vertical;line-height:1.4;font-family:inherit;"));
    const l = document.createElement("button");
    ((l.type = "button"),
      (l.textContent = "Import"),
      (l.style.cssText =
        "background:#f3f4f6;border:1px solid #d1d5db;color:#374151;border-radius:6px;padding:6px 14px;font-size:12px;font-weight:600;cursor:pointer;white-space:nowrap;flex-shrink:0;"),
      l.addEventListener("click", () => {
        (r.value
          .split(",")
          .map((e) => e.trim())
          .filter(Boolean)
          .forEach((e) => s({ school_name: e, school_type: "interviewed" })),
          (r.value = ""),
          (c.scrollTop = c.scrollHeight));
      }),
      i.appendChild(r),
      i.appendChild(l),
      t.appendChild(i));
    const d = document.createElement("div");
    ((d.style.cssText =
      "display:grid;grid-template-columns:1fr 130px 32px;gap:8px;font-size:11px;font-weight:600;color:#9ca3af;padding:0 2px 6px;border-bottom:1px solid #f0f0f0;margin-bottom:6px;"),
      (d.innerHTML = "<span>School Name</span><span>Type</span><span></span>"),
      t.appendChild(d));
    const c = document.createElement("div");
    function s(e) {
      e = e || {};
      const t = document.createElement("div");
      ((t.className = "adm-school-row"),
        (t.style.cssText =
          "display:grid;grid-template-columns:1fr 130px 32px;gap:8px;align-items:center;"));
      const n = document.createElement("input");
      ((n.type = "text"),
        (n.className = "adm-input"),
        (n.placeholder = "School name"),
        (n.value = e.school_name || ""),
        (n.style.cssText = "padding:5px 8px;font-size:12px;"));
      const a = document.createElement("select");
      ((a.className = "adm-select"),
        (a.style.cssText = "padding:5px 8px;font-size:12px;"),
        [
          ["interviewed", "Interviewed"],
          ["accepted", "Accepted"],
          ["scholarship", "Scholarship"],
        ].forEach(([t, n]) => {
          const o = document.createElement("option");
          ((o.value = t),
            (o.textContent = n),
            t === (e.school_type || "interviewed") && (o.selected = !0),
            a.appendChild(o));
        }));
      const o = document.createElement("button");
      ((o.type = "button"),
        (o.innerHTML = "&times;"),
        (o.style.cssText =
          "background:#fff;border:1.5px solid #fca5a5;color:#ef4444;border-radius:4px;width:28px;height:28px;cursor:pointer;font-size:16px;display:flex;align-items:center;justify-content:center;flex-shrink:0;"),
        o.addEventListener("click", () => t.remove()),
        t.appendChild(n),
        t.appendChild(a),
        t.appendChild(o),
        c.appendChild(t));
    }
    return (
      (c.style.cssText =
        "display:flex;flex-direction:column;gap:6px;max-height:260px;overflow-y:auto;padding-right:2px;"),
      t.appendChild(c),
      e.forEach(s),
      {
        el: t,
        getSchools: function () {
          return Array.from(c.querySelectorAll(".adm-school-row"))
            .map((e, t) => ({
              school_name: e.querySelector("input").value.trim(),
              school_type: e.querySelector("select").value,
              sort_order: t + 1,
            }))
            .filter((e) => e.school_name);
        },
      }
    );
  }
  function H() {
    const e = S("adm-new-app-modal"),
      t = e.querySelector(".adm-modal");
    ((t.innerHTML =
      '\n      <div class="adm-modal-header">\n        <span class="adm-modal-title">Add New Application</span>\n        <button class="adm-modal-close" aria-label="Close">&times;</button>\n      </div>'),
      t
        .querySelector(".adm-modal-close")
        .addEventListener("click", () => N(e)));
    const n = document.createElement("div");
    n.className = "adm-modal-body";
    const a = document.createElement("div");
    a.className = "adm-form-grid";
    const o = L("text"),
      r = L("text"),
      l = L("text"),
      d = L("text"),
      c = L("number", "", "0.01", "0", "4.0"),
      s = L("number", "", null, "472", "528"),
      p = L("number", 0, null, "0"),
      u = L("number", 0, null, "0"),
      h = L("number", 0, null, "0"),
      y = L("number", 0, null, "0"),
      v = L("number", "", null, "2000", "2100"),
      x = L("number", "", null, "2000", "2100"),
      C = L("text"),
      g = T(
        [
          ["published", "Published"],
          ["draft", "Draft"],
          ["archived", "Archived"],
        ],
        "published",
      ),
      b = {
        pseudonym: o,
        alias: r,
        undergrad_institution: l,
        state: d,
        gpa: c,
        mcat: s,
        gap_years: p,
        interviews_count: u,
        acceptances_count: h,
        scholarships_count: y,
        application_cycle_start_year: v,
        application_cycle_end_year: x,
        ses_disadvantaged: C,
        status: g,
      };
    (a.appendChild(I("Identity")),
      a.appendChild(A("Pseudonym *", o)),
      a.appendChild(A("Alias *", r)),
      a.appendChild(I("Academic")),
      a.appendChild(A("Undergrad Institution *", l, !0)),
      a.appendChild(A("State *", d)),
      a.appendChild(A("GPA *", c)),
      a.appendChild(A("MCAT *", s)),
      a.appendChild(I("Application Cycle")),
      a.appendChild(A("Cycle Start Year *", v)),
      a.appendChild(A("Cycle End Year *", x)),
      a.appendChild(A("Gap Years", p)),
      a.appendChild(I("Outcomes")),
      a.appendChild(A("Interviews", u)),
      a.appendChild(A("Acceptances", h)),
      a.appendChild(A("Scholarships", y)),
      a.appendChild(I("Profile")),
      a.appendChild(A("SES Disadvantaged *", C, !0)),
      a.appendChild(A("Status", g)));
    const E = {};
    (a.appendChild(I("Flags")),
      [
        ["on_time_application", "On-Time Application", !1],
        ["reapplicant", "Reapplicant", !1],
        ["first_generation", "First Generation", !1],
        ["military_service", "Military Service", !1],
        ["institutional_actions", "Institutional Actions", !1],
        ["is_public", "Is Public", !0],
      ].forEach(([e, t, n]) => {
        const { wrap: o, chk: i } = M(t, n);
        ((E[e] = i), a.appendChild(o));
      }));
    const { el: _, getSchools: q } = F([]);
    (a.appendChild(_), n.appendChild(a), t.appendChild(n));
    const P = document.createElement("div");
    P.className = "adm-modal-footer";
    const H = document.createElement("button");
    ((H.className = "adm-btn-cancel"),
      (H.textContent = "Cancel"),
      H.addEventListener("click", () => N(e)));
    const O = document.createElement("button");
    ((O.className = "adm-btn-save"),
      (O.textContent = "Create Application"),
      O.addEventListener("click", async () => {
        let t = !1;
        ([o, r, l, d, C].forEach((e) => {
          e.value.trim()
            ? (e.style.borderColor = "")
            : ((e.style.borderColor = "#ef4444"), t || (e.focus(), (t = !0)));
        }),
          [v, x].forEach((e) => {
            "" === e.value
              ? ((e.style.borderColor = "#ef4444"), t || (e.focus(), (t = !0)))
              : (e.style.borderColor = "");
          }));
        const n = parseFloat(c.value);
        "" === c.value || isNaN(n) || n < 0 || n > 4
          ? ((c.style.borderColor = "#ef4444"),
            (c.title = "GPA must be between 0.00 and 4.00"),
            t || (c.focus(), (t = !0)))
          : ((c.style.borderColor = ""), (c.title = ""));
        const a = parseInt(s.value, 10);
        if (
          ("" === s.value || isNaN(a) || a < 472 || a > 528
            ? ((s.style.borderColor = "#ef4444"),
              (s.title = "MCAT must be between 472 and 528"),
              t || (s.focus(), (t = !0)))
            : ((s.style.borderColor = ""), (s.title = "")),
          t)
        )
          return;
        ((O.disabled = !0), (O.textContent = "Creating…"));
        const p = {};
        (Object.entries(b).forEach(([e, t]) => {
          p[e] =
            "SELECT" === t.tagName ? t.value : "" === t.value ? null : t.value;
        }),
          Object.entries(E).forEach(([e, t]) => {
            p[e] = t.checked;
          }),
          ["gpa"].forEach((e) => {
            null !== p[e] && (p[e] = parseFloat(p[e]));
          }),
          [
            "mcat",
            "gap_years",
            "interviews_count",
            "acceptances_count",
            "scholarships_count",
            "application_cycle_start_year",
            "application_cycle_end_year",
          ].forEach((e) => {
            null !== p[e] && (p[e] = parseInt(p[e], 10));
          }));
        const u = await D(i, p);
        if (u) {
          const t = u.application || u,
            n = q();
          if (n.length && t.id) {
            const e = await j(t.id, n);
            e && (t.application_schools = e.schools || []);
          }
          (m.unshift(t), (f = 1), w(), N(e));
        } else
          ((O.disabled = !1),
            (O.textContent = "Create Application"),
            alert("Failed to create. Please try again."));
      }),
      P.appendChild(H),
      P.appendChild(O),
      t.appendChild(P),
      k(e));
  }
  function O() {
    const e = S("adm-new-exp-modal"),
      t = e.querySelector(".adm-modal");
    ((t.innerHTML =
      '\n      <div class="adm-modal-header">\n        <span class="adm-modal-title">Add New Experience</span>\n        <button class="adm-modal-close" aria-label="Close">&times;</button>\n      </div>'),
      t
        .querySelector(".adm-modal-close")
        .addEventListener("click", () => N(e)));
    const n = document.createElement("div");
    n.className = "adm-modal-body";
    const a = document.createElement("div");
    a.className = "adm-form-grid";
    const o = T(
        [["", "— Select application —"]].concat(
          m.map((e) => [e.id, e.pseudonym || e.alias || e.id]),
        ),
        "",
      ),
      i = L("text"),
      r = L("text"),
      l = T(
        [
          ["", "— Select type —"],
          ["additional", "Additional"],
          ["most_meaningful", "Most Meaningful"],
        ],
        "",
      ),
      c = L("number", 0, null, "0"),
      s = document.createElement("textarea");
    ((s.className = "adm-textarea"),
      (s.rows = 5),
      a.appendChild(A("Application (Student) *", o, !0)),
      a.appendChild(A("Title *", i, !0)),
      a.appendChild(A("Category *", r)),
      a.appendChild(A("Type *", l)),
      a.appendChild(A("Hours", c)),
      a.appendChild(A("Description", s, !0)),
      n.appendChild(a),
      t.appendChild(n));
    const p = document.createElement("div");
    p.className = "adm-modal-footer";
    const u = document.createElement("button");
    ((u.className = "adm-btn-cancel"),
      (u.textContent = "Cancel"),
      u.addEventListener("click", () => N(e)));
    const f = document.createElement("button");
    ((f.className = "adm-btn-save"),
      (f.textContent = "Create Experience"),
      f.addEventListener("click", async () => {
        let t = !1;
        if (
          ([o, i, r, l].forEach((e) => {
            e.value && e.value.trim()
              ? (e.style.borderColor = "")
              : ((e.style.borderColor = "#ef4444"), t || (e.focus(), (t = !0)));
          }),
          t)
        )
          return;
        ((f.disabled = !0), (f.textContent = "Creating…"));
        const n = m.find((e) => e.id === o.value),
          a =
            h
              .filter((e) => e.application_id === o.value)
              .reduce((e, t) => Math.max(e, t.sort_order || 0), 0) + 1,
          p = {
            application_id: o.value,
            title: i.value || null,
            experience_type: r.value || null,
            category: l.value || null,
            hours: "" !== c.value ? parseInt(c.value, 10) : null,
            sort_order: a,
            description: s.value || null,
          },
          u = await D(d, p);
        if (u) {
          const t = {
            ...(u.experience || u),
            student_name: (n && (n.pseudonym || n.alias)) || "-",
          };
          (h.unshift(t), (y = 1), E(), N(e));
        } else
          ((f.disabled = !1),
            (f.textContent = "Create Experience"),
            alert("Failed to create. Please try again."));
      }),
      p.appendChild(u),
      p.appendChild(f),
      t.appendChild(p),
      k(e));
  }
  async function B() {
    const t = await (async function () {
      try {
        const t = await fetch(n, {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
              apikey: e,
              Authorization: "Bearer " + e,
            },
            body: JSON.stringify({
              user_id:
                "undefined" != typeof CURRENT_USER_ID ? CURRENT_USER_ID : null,
              filters: {},
            }),
          }),
          a = await t.json();
        return t.ok
          ? a.applications || []
          : (console.error("Failed to load applications:", a), []);
      } catch (e) {
        return (console.error("Fetch applications error:", e), []);
      }
    })();
    ((m = t),
      (h = t.flatMap((e) =>
        (Array.isArray(e.application_experiences)
          ? e.application_experiences
          : []
        ).map((t) => ({
          ...t,
          pseudonym: e.pseudonym || "-",
          student_name: e.pseudonym || "-",
        })),
      )),
      (f = 1),
      (y = 1),
      w(),
      E());
  }
  function z(e) {
    if (!e) return "";
    const t = e.querySelector('input[type="search"], input[type="text"]');
    return t ? t.value.trim().toLowerCase() : "";
  }
  function U(e) {
    if ("experiences" === e) {
      return z(document.querySelector('form[data-search-form="experiences"]'));
    }
    const t = document.querySelector('form[data-search-form="normal"]'),
      n = document.querySelector('form[data-search-form="sidebar-normal"]');
    return z(t) || z(n) || "";
  }
  function J(e) {
    if ("experiences" === e) return ((y = 1), void E());
    ((f = 1), w());
  }
  function G(e) {
    if (!(e instanceof HTMLFormElement)) return;
    const t = e.getAttribute("data-search-form");
    if (!t) return;
    J("experiences" === t ? "experiences" : "normal");
  }
  function R(e) {
    const t =
        "experiences" === e
          ? '[data-action="new-experience"]'
          : '[data-action="new-application"]',
      n = document.querySelector(t);
    if (n) return n;
    const a = document.querySelectorAll(
        "a, button, div[role='button'], .w-button",
      ),
      o =
        "experiences" === e
          ? ["add new experience", "new experience"]
          : ["add new application", "add new"];
    for (let e = 0; e < a.length; e++) {
      const t = a[e],
        n = (t.textContent || "").trim().toLowerCase();
      if (o.some((e) => n.includes(e))) return t;
    }
    return null;
  }
  function $(t) {
    if (document.getElementById(t.id)) return;
    const n = t.addNewButton;
    if (!n || !n.parentElement) return;
    const a = document.createElement("button");
    ((a.id = t.id),
      (a.type = "button"),
      (a.textContent = t.label),
      (a.className = n.className || ""),
      (a.style.marginLeft = "8px"));
    const o = document.createElement("input");
    ((o.type = "file"),
      (o.accept =
        ".csv,text/csv,application/vnd.ms-excel,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,.xls,.xlsx"),
      (o.style.display = "none"),
      a.addEventListener("click", function (e) {
        (e.preventDefault(), e.stopPropagation(), o.click());
      }),
      o.addEventListener("change", async function () {
        const n = o.files && o.files[0];
        if (((o.value = ""), !n)) return;
        a.disabled = !0;
        const i = a.textContent;
        a.textContent = "Uploading...";
        try {
          const a = await (async function (t, n) {
            const a = new FormData();
            a.append("file", t);
            const o = await fetch(n, {
              method: "POST",
              headers: { apikey: e, Authorization: "Bearer " + e },
              body: a,
            });
            let i = null;
            try {
              i = await o.json();
            } catch (e) {
              i = null;
            }
            if (o.ok) return i || { success: !0, inserted: 0, failed: 0 };
            let r = "Failed to upload spreadsheet file.";
            try {
              r = (function (e) {
                if (!e || "object" != typeof e)
                  return "Upload failed. Please check your file format and required columns.";
                const t = e.message || e.error,
                  n = Array.isArray(e.errors) ? e.errors : [];
                if (!n.length)
                  return (
                    t ||
                    "Upload failed. Please check your file format and required columns."
                  );
                const a = n.slice(0, 6).map(function (e) {
                  return (
                    (e && void 0 !== e.row && null !== e.row
                      ? "Row " + e.row
                      : "Row ?") +
                    ": " +
                    (function (e) {
                      const t = String(e || "").trim(),
                        n = t.toLowerCase();
                      if (n.includes("invalid app cycle format"))
                        return "App Cycle must look like 2023-2024.";
                      if (n.includes("pseudonym is required"))
                        return "Missing required field: Pseudonym.";
                      if (n.includes("title is required"))
                        return "Missing required field: Title.";
                      if (n.includes("experience_type is required"))
                        return "Missing required field: Experience Type.";
                      if (n.includes("could not resolve application_id"))
                        return "Applicant not found. Provide a valid application_id or matching Pseudonym/Alias.";
                      if (
                        n.includes(
                          "application_experiences_application_id_fkey",
                        )
                      )
                        return "Invalid application_id (no matching application record).";
                      return t;
                    })(String(e && e.error ? e.error : "Invalid value"))
                  );
                });
                n.length > 6 &&
                  a.push("...and " + (n.length - 6) + " more row errors.");
                return a.join("\n");
              })(i || {});
            } catch (e) {
              try {
                const e = await o.text();
                e && (r = e);
              } catch (e) {}
            }
            throw new Error(r);
          })(n, t.endpoint);
          t.onSuccess && (await t.onSuccess(a));
          const o = Number(a && a.failed ? a.failed : 0);
          if (o > 0) {
            const e =
                Array.isArray(a.errors) && a.errors.length ? a.errors[0] : null,
              t = e
                ? " First error row " +
                  e.row +
                  ": " +
                  (e.error || "Unknown error")
                : "";
            alert(
              "Upload finished with issues. Inserted: " +
                (a.inserted || 0) +
                ", Failed: " +
                o +
                "." +
                t,
            );
          } else
            alert(
              "Upload successful. Inserted: " +
                (a && a.inserted ? a.inserted : 0),
            );
        } catch (e) {
          alert(
            e && e.message ? e.message : "Failed to upload spreadsheet file.",
          );
        } finally {
          ((a.disabled = !1), (a.textContent = i));
        }
      }),
      n.insertAdjacentElement("afterend", a),
      document.body.appendChild(o));
  }
  async function X0(t) {
    if (!t) return !1;
    try {
      const n = await fetch(Y, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          apikey: e,
          Authorization: "Bearer " + e,
        },
        body: JSON.stringify({ user_id: t }),
      });
      if (!n.ok) return !1;
      const a = await n.json().catch(() => ({}));
      return !!(a && !0 === a.is_admin);
    } catch (e) {
      return (console.error("Admin check failed:", e), !1);
    }
  }
  ((window.openNewAppModal = H),
    (window.openNewExpModal = O),
    document.addEventListener("DOMContentLoaded", async function () {
      var e,
        t,
        n = {};
      try {
        n = JSON.parse(localStorage.getItem("portal_user") || "{}") || {};
      } catch (e) {
        return void window.location.replace("/portal");
      }
      const a0 = localStorage.getItem("portal_user_id") || n.id || null;
      if (!a0) return void window.location.replace("/portal");
      if (!(await X0(a0))) return void window.location.replace("/portal");
      function a(e, t, n) {
        let a = !1;
        (document.querySelectorAll(e).forEach((e) => {
          (e.addEventListener("click", (e) => {
            (e.preventDefault(), e.stopPropagation(), n());
          }),
            (a = !0));
        }),
          a ||
            document
              .querySelectorAll("a, button, div[role='button'], .w-button")
              .forEach((e) => {
                e.textContent.trim().toLowerCase().includes(t) &&
                  e.addEventListener("click", (e) => {
                    (e.preventDefault(), e.stopPropagation(), n());
                  });
              }));
      }
      ((t =
        ((e = n || {}).name && e.name.trim()) ||
        (e.full_name && e.full_name.trim()) ||
        [e.first_name, e.last_name].filter(Boolean).join(" ").trim() ||
        (e.email && e.email.trim()) ||
        "User Name"),
        document
          .querySelectorAll('[data-field="current-user-name"]')
          .forEach(function (e) {
            e.textContent = t;
          }),
        (function () {
          if (!document.getElementById("sidebar-admin-link")) {
            var e = document.createElement("a");
            ((e.id = "sidebar-admin-link"),
              (e.className = "sidebar-admin-link"),
              (e.href = "/portal-admin"),
              (e.innerHTML =
                '<svg xmlns="http://www.w3.org/2000/svg" width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg><span class="sidebar-text sidebar-admin-link-text">Admin Dashboard</span>'));
            var t = document.querySelector(".sidebar-nav");
            if (t) t.appendChild(e);
            else {
              var n = document.querySelector(".sidebar");
              n && n.appendChild(e);
            }
          }
        })(),
        a('[data-action="new-application"]', "add new application", H),
        a('[data-action="new-experience"]', "add new experience", O),
        document.addEventListener("click", function (e) {
          const t = e.target.closest('[data-action="search-normal"]'),
            n = e.target.closest(
              '[data-action="search-experiences"], [data-action="search-favorite"]',
            );
          t ? J("normal") : n && J("experiences");
        }),
        document.addEventListener("submit", function (e) {
          const t = e.target;
          t instanceof HTMLFormElement &&
            t.getAttribute("data-search-form") &&
            (e.preventDefault(), G(t));
        }),
        document.addEventListener("input", function (e) {
          const t = e.target;
          if (!(t instanceof HTMLInputElement)) return;
          if ("search" !== t.type && "text" !== t.type) return;
          const n = t.closest("form");
          n instanceof HTMLFormElement &&
            n.getAttribute("data-search-form") &&
            "" === t.value.trim() &&
            G(n);
        }),
        document.addEventListener(
          "keydown",
          function (e) {
            if ("Enter" !== e.key) return;
            const t = e.target;
            if (!(t instanceof HTMLInputElement)) return;
            if ("search" !== t.type && "text" !== t.type) return;
            const n = t.closest("form");
            n instanceof HTMLFormElement &&
              n.getAttribute("data-search-form") &&
              (e.preventDefault(), e.stopPropagation(), G(n));
          },
          !0,
        ),
        $({
          id: "adm-upload-applications-btn",
          label: "Upload Applications",
          kind: "applications",
          endpoint: s,
          addNewButton: R("applications"),
          onSuccess: async function () {
            await B();
          },
        }),
        $({
          id: "adm-upload-experiences-btn",
          label: "Upload Experiences",
          kind: "experiences",
          endpoint: p,
          addNewButton: R("experiences"),
          onSuccess: async function () {
            await B();
          },
        }),
        B());
    }));
})();
